<% @teams = Team.all %>


<%= form_for :game do |f| %>
  <%= f.label :date, 'Date - (ex: 2014-07-27 17:00)' %>:
  <%= f.text_field :date %><br />

  <%= f.label :location %>:
  <%= f.text_field :location %><br />

  <%= hidden_field_tag 'game[team1_id]', current_user.primary_team_id %>

  <%= f.label :team2_id, 'Play Against' %>:

 <%= f.select(:team2_id, options_for_select(@teams.pluck(:name)), {}, class: "browser-default") %>
    <%# @teams.each do |team| %>
      <%#= content_tag(:option, team.name, value: team.id) %>
    <%# end %>
  <%# end %>
  <%= f.submit %>
<% end %>
